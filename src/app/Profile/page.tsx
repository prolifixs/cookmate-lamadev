'use client'

import ProfileLayout from '@/components/ProfileComponents/ProfileLayout';
import ProfileContent from '@/components/ProfileComponents';

export default function ProfilePage() {
  return (
    <ProfileLayout>
      <ProfileContent />
    </ProfileLayout>
  );
}
